  <section class="flex flex-col gap-4 rounded-md bg-stone-900/65 p-5 md:flex-row md:items-center md:justify-between">
    <div class="space-y-1">
      <h2 class="text-lg font-semibold text-stone-100">Explore the report</h2>
      <p class="text-sm text-stone-400">Bounce between the friendly list and the data-dense table whenever you need.</p>
    </div>
    <div class="inline-flex items-center gap-3 rounded-md bg-stone-950/70 p-1.5">
      <button
        id="btnList"
        type="button"
        class="view-toggle rounded-sm px-5 py-2.5 text-sm font-medium transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-stone-500">
        List View
      </button>
      <button
        id="btnBlocks"
        type="button"
        class="view-toggle rounded-sm px-5 py-2.5 text-sm font-medium transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-stone-500">
        Blocks View
      </button>
      <button
        id="btnTable"
        type="button"
        class="view-toggle rounded-sm px-5 py-2.5 text-sm font-medium transition-colors focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-stone-500">
        Table View
      </button>
    </div>
  </section>
